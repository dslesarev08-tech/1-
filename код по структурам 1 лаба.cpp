/*
 * Автор: Слесарев Денис Владимирович
 * Группа: 090301-ПОВа-о25
 * 
 * Задача: Представить число N в виде суммы двух натуральных чисел A и B,
 *         таких что НОД(A, B) максимален.
 */

#include <iostream>
#include <cmath>

using namespace std;

int main() {
    // Вывод информации об авторе
    cout << "Автор: Слесарев Денис Владимирович" << endl;
    cout << "090301-ПОВа-о25" << endl;
    cout << "-----------------------------" << endl;
    
    long long N;
    cin >> N;
    
    long long A, B;
    
    // Идея: чтобы НОД(A, B) был максимальным, нужно, чтобы A и B были как можно больше
    // и делились на один и тот же большой делитель.
    // Оптимальное решение: пусть d = НОД(A, B). Тогда A = d * a, B = d * b, где НОД(a, b) = 1.
    // Тогда N = d * (a + b). Значит d является делителем N, и a + b = N/d.
    // Чтобы d был максимальным, нужно выбрать максимальный делитель N, который меньше N.
    // Но также нужно, чтобы a и b были натуральными и НОД(a, b) = 1.
    // Наибольший возможный d = N/2, но это возможно только если N чётное.
    
    if (N % 2 == 0) {
        // Для чётного N максимальный НОД = N/2
        A = N / 2;
        B = N / 2;
    } else {
        // Для нечётного N нужно найти максимальный делитель, отличный от N
        // Максимальный НОД будет равен наибольшему делителю числа N, меньшему N
        // Это N / p, где p - наименьший простой делитель N
        
        long long minDivisor = N;
        
        // Находим наименьший простой делитель N
        for (long long i = 2; i * i <= N; i++) {
            if (N % i == 0) {
                minDivisor = i;
                break;
            }
        }
        
        // Если N простое, то minDivisor = N
        if (minDivisor == N) {
            // Для простого N максимальный НОД = 1
            A = 1;
            B = N - 1;
        } else {
            // Иначе максимальный НОД = N / minDivisor
            long long d = N / minDivisor;
            A = d;
            B = N - d;
        }
    }
    
    // Вывод результата
    cout << A << " " << B << endl;
    
    return 0;
}